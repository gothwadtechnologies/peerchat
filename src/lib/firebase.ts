// Serverless P2P Firebase Mock
// Replaces Firebase Cloud Messaging web integrations cleanly for a complete standalone build.

export const getFirebaseApp = () => null;

export const getFirebaseMessaging = async () => null;
